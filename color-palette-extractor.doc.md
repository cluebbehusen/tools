# color-palette-extractor

Extracts a compact set of dominant colors from an image.

- Input: drag/drop or choose an image file (runs locally).
- Output: palette swatches with hex codes and a copyable hex list.
- Controls: number of colors and optional lightness sorting.
- Note: palette can vary slightly each time due to randomized sampling.
